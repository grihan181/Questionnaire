package com.opencode.practice.comparator;

import com.opencode.practice.assistClass.UserScore;

import java.util.Comparator;

public class LeaderBoardComparator implements Comparator<UserScore> {
    @Override
    public int compare(UserScore o1, UserScore o2) {
        return 0;
    }
}
